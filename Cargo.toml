[package]
name = "svarm"
version = "0.1.0"
edition = "2024"
description = "A small terminal multiplexer for coding agents"

[dependencies]
clap = { version = "4.5", features = ["derive"] }
portable-pty = "0.9"
tui-term = "0.3"
